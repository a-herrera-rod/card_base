import '../../game_controller.dart';
import 'cards_array.dart';

class Bench extends CardsArray {
  Bench(GameController gameController, double x, double y, double spacing)
      : super(gameController, x, y, spacing);
}
